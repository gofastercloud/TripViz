# Core (required)
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pillow==10.4.0
aiofiles==24.1.0
python-multipart==0.0.12

# Optional: face detection + clustering
# pip install mediapipe scikit-learn
# mediapipe>=0.10.14
# scikit-learn>=1.3.0

# Optional: activity detection via CLIP (downloads ~340 MB model on first use)
# pip install transformers torch
# transformers>=4.40.0
# torch>=2.0.0

# Optional: macOS HEIC/HEIF support (iPhone photos)
# pip install pillow-heif
